# Postgres
POSTGRES_DB=financedash
POSTGRES_USER=financedash
POSTGRES_PASSWORD=financedash
POSTGRES_PORT=5432

# Backend
BACKEND_PORT=8080

# Frontend
FRONTEND_PORT=5173
# Base URL the browser uses to reach the backend (baked into the frontend build).
VITE_API_BASE_URL=http://localhost:8080
