# Future Work — Claude Code workflow tooling

Placeholder log of dev-workflow ideas to build out later. Entries are intentionally terse reminders
for the user, not specs — flesh out when picked up.

- Skill: recommend a model (Sonnet/Opus/Haiku) for an implementation task before work starts, based
  on task analysis (ambiguity, novelty, risk, volume).
- Skill: validate/check a branch name against the feature actually implemented on it, flagging
  mismatches (e.g. a stale or misleading name).
